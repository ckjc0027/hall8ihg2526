import TournamentManager from './tournament-manager';
// Import the styling needed for your component (optional, but good practice)
import './App.css';

function App() {
  // We return the TournamentManager component, which is your entire application.
  return (
    <TournamentManager />
  );
}

export default App;